// Community edition stub — cluster detection is available in WarmReach Pro.
export function useConnectionClusters() {
  return {
    data: null,
    isLoading: false,
    error: null,
    refetch: async () => {},
  };
}
