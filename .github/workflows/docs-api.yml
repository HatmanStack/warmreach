name: API Docs Build

# Proves the generated API docs (typedoc for TS, mkdocstrings for Python) stay
# buildable from source. It does not publish — the output is generated in CI and
# discarded, so reference docs cannot silently break when source changes.

on:
  push:
    branches: [main]
    paths:
      - 'frontend/src/**'
      - 'client/src/**'
      - 'admin/src/**'
      - 'backend/lambdas/shared/python/**'
      - 'typedoc.base.json'
      - '*/typedoc.json'
      - 'backend/mkdocs.yml'
      - 'backend/docs-api/**'
      - 'package.json'
      - '.github/workflows/docs-api.yml'
  pull_request:
    branches: [main]
    paths:
      - 'frontend/src/**'
      - 'client/src/**'
      - 'admin/src/**'
      - 'backend/lambdas/shared/python/**'
      - 'typedoc.base.json'
      - '*/typedoc.json'
      - 'backend/mkdocs.yml'
      - 'backend/docs-api/**'
      - 'package.json'
      - '.github/workflows/docs-api.yml'

permissions:
  contents: read

jobs:
  docs-api:
    name: Build API Docs
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6
      - uses: actions/setup-node@48b55a011bda9f5d6aeb4c2d9c7362e8dae4041e # v6
        with:
          node-version: '24'
      - uses: actions/setup-python@ece7cb06caefa5fff74198d8649806c4678c61a1 # v6
        with:
          python-version: '3.13'
      - name: Install uv
        run: pip install uv
      - name: Install dependencies
        run: npm ci
      - name: Generate TypeScript API docs
        run: npm run docs:api:ts
      - name: Generate Python API docs
        run: npm run docs:api:py
