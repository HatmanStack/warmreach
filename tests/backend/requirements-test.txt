# Source manifest for the backend test dependencies (loose version ranges).
#
# NOT authoritative for installs. `requirements-test.lock` (pinned + hashed) is
# the single source of truth that both CI (.github/workflows/ci.yml) and local
# setup (scripts/setup.sh) install from. This file exists only as the input to
# regenerate the lock:
#
#     uv pip compile requirements-test.txt -o requirements-test.lock
#
# Edit a dependency here, then recompile the lock and commit both.
pytest>=9.1.1
pytest-mock>=3.15.1
pytest-cov>=7.1.0
moto>=5.2.2
requests-mock>=1.12.1
boto3>=1.43.50
openai>=2.46.0
PyJWT[crypto]>=2.13.0
stripe>=15.3.0
mypy>=2.3.0
